<h1> <%= @place.name %> </h1>

<h2>Post</h2>
    <ul>
        <% for post in @place.posts%>
            <h3> <%= post.title %> </h3>
            <em> <%= post.posted_on %> </em>
            <p><%= post.description %> </p>            
        <% end %>
    </ul>


<a href="/posts/post?place_id=<%=@place.id%>"> Add a post</a>

<em>
    <%= button_to "Delete place", @place, method: :delete%>
</em>

<button type="button"><a href="/places"> Back to all plaaces</a>